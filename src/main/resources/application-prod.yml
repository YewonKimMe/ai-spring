spring:
  application:
    name: AiProject
  # LANGCHAIN
  lang-chain:
    open-ai:
      # OPEN-AI API-KEY
      api-key: ${API_KEY}
  datasource:
    # DB URL
    url: ${DB_URL}
    # DB USERNAME
    username: ${DB_USERNAME}
    # DB PASSWORD
    password: ${DB_PASSWORD}